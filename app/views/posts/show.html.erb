<p id="notice"><%= notice %></p>

<div class="container">
<div class="col-md-5 col-md-offset-3">
<div class="row">
    <h1><%= @post.title %></h1>
</div>
<br><br>
<div class="row">
<p><%= image_tag @post.image.url(:medium) %></p>
<br>
<p><%= @post.description %></p>
<% if user_signed_in? %>
<p><%= link_to 'Edit', edit_post_path(@post), class: 'btn btn-primary' %> |
<% end %>
<%= link_to 'Back', posts_path, class: 'btn btn-primary' %></p>
</div>
</div>
</div>

